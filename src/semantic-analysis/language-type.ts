/**
 * Types carried by `Type` in the grammar (`int`, `string`, `boolean`).
 * Phase B uses these for symbol entries and compatibility checks.
 */
export type LanguageType = "int" | "string" | "boolean";
